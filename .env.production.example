# Production Database Configuration
# Copy this file to .env.production and fill in your production credentials
# DO NOT commit .env.production to git!

# Production Database Host (e.g., mysql.yourdomain.com or IP address)
PROD_DB_HOST="your-production-host.com"

# Production Database User
PROD_DB_USER="dieselsu_dbuser"

# Production Database Password
PROD_DB_PASS="your-production-password"

# Production Database Name
PROD_DB_NAME="dieselsu_faqs"

# Optional: SSH Tunnel (if production requires SSH tunnel)
# PROD_SSH_HOST="your-ssh-host.com"
# PROD_SSH_USER="your-ssh-user"
# PROD_SSH_PORT="22"
