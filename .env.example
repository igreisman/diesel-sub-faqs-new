# Database Configuration for Submarine FAQ Application
# Copy this file to .env.local for local development
# Set these as environment variables in Vercel for production

# Database Connection (choose one option)

# Option 1: Individual connection parameters
DB_HOST=your-database-host
DB_USER=your-database-username  
DB_PASSWORD=your-database-password
DB_NAME=submarine_faqs

# Option 2: Connection string (for cloud database services)
# DATABASE_URL=mysql://username:password@host:port/database

# Example for PlanetScale:
# DATABASE_URL=mysql://username:password@aws.connect.psdb.cloud/submarine-faqs?ssl={"rejectUnauthorized":true}

# Example for local MySQL:
# DATABASE_URL=mysql://root:password@localhost:3306/submarine_faqs

# Admin Authentication
ADMIN_PASSWORD=1945

# Environment
NODE_ENV=production