services:
  app:
    build:
      context: .
      dockerfile: docker/app/Dockerfile
    image: submarine-faqs-app
    container_name: submarine-faqs-app
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    ports:
      - "8080:80"
    environment:
      # These align with config/database.php fallbacks
      MYSQLHOST: db
      MYSQLPORT: 3306
      MYSQLUSER: dieselsu_dbuser
      MYSQLPASSWORD: codjuw-xojWo6-datqem
      MYSQLDATABASE: dieselsu_faqs
    depends_on:
      - db
  phpmyadmin:
    image: phpmyadmin:5
    container_name: submarine-faqs-pma
    restart: unless-stopped
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_USER: dieselsu_dbuser
      PMA_PASSWORD: codjuw-xojWo6-datqem
    depends_on:
      - db

  db:
    build: ./database
    image: submarine-faqs-db
    container_name: submarine-faqs-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: dieselsu_faqs
      MYSQL_USER: dieselsu_dbuser
      MYSQL_PASSWORD: codjuw-xojWo6-datqem
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3307:3306" # optional host access

volumes:
  db_data:
